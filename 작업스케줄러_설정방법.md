# Windows 작업 스케줄러로 임원인사 브리핑 파이프라인 자동 실행

## 1. 작업 스케줄러 열기

- **Win + R** → `taskschd.msc` 입력 → Enter  
  또는  
- **시작 메뉴**에서 "작업 스케줄러" 검색 후 실행

---

## 2. 새 작업 만들기

1. 오른쪽 **"작업 만들기..."** 클릭 (기본 작업이 아님)
2. **일반** 탭
   - 이름: `임원인사 브리핑 파이프라인` (원하는 이름으로)
   - "가장 높은 권한으로 실행" 필요 시 체크 (보통 불필요)
   - "사용자가 로그온할 때만 실행" 또는 "사용자 로그온 여부에 관계없이 실행" 중 선택

---

## 3. 트리거(실행 시각) 설정

1. **트리거** 탭 → **새로 만들기**
2. 예: **매일 오전 9시**
   - 작업 시작: **일정**
   - 설정: **매일**
   - 시간: **오전 9:00:00** (원하는 시간으로)
   - 간격: **1일**
3. **확인** 클릭

---

## 4. 동작(실행할 프로그램) 설정

1. **동작** 탭 → **새로 만들기**
2. 다음처럼 입력:
   - **동작**: "프로그램 시작"
   - **프로그램/스크립트**:
     ```
     C:\Users\USER\.cursor\daily-exec-news\run_briefing_pipeline.bat
     ```
   - **시작 위치(선택)** (되도록 채우기):
     ```
     C:\Users\USER\.cursor\daily-exec-news
     ```
3. **확인** 클릭

---

## 5. 조건·설정 (선택)

- **조건** 탭: "컴퓨터의 전원이 AC일 때만 작업 시작" 등 필요 시 해제
- **설정** 탭: "작업이 실패하면 다시 시작 간격" 등 필요 시 설정

---

## 6. 저장 후 확인

- **확인** 클릭해 작업 저장
- 목록에서 해당 작업 선택 → **실행**으로 한 번 수동 실행해 보기
- `C:\Users\USER\.cursor\daily-exec-news` 폴더에 `news_raw.json`, `news_summary.json`, `email_samsung_hyundai.json` 등이 갱신되는지 확인

---

## 참고

- **환경 변수**: 스크립트가 `.env`에서 `OPENAI_API_KEY`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` 등을 읽는다면, 작업 스케줄러는 **해당 사용자로 로그온한 상태**에서 실행할 때 그 사용자 환경을 따릅니다. "사용자 로그온 여부에 관계없이 실행"이면 시스템 계정으로 돌아가서 `.env` 경로나 환경이 다를 수 있으니, 문제 있으면 "사용자가 로그온할 때만 실행"으로 두고 테스트해 보세요.
- **실행 경로**: `run_briefing_pipeline.bat`을 쓰면 배치 파일이 자동으로 `daily-exec-news` 폴더로 이동한 뒤 `python run_briefing_pipeline.py`를 실행합니다.
- **로그**: 실행 결과를 파일로 남기려면 동작에서 "프로그램/스크립트"를 `cmd`로, "인수 추가"를 `/c "run_briefing_pipeline.bat >> pipeline_log.txt 2>&1"`처럼 설정하는 방식으로 로그를 남길 수 있습니다.
